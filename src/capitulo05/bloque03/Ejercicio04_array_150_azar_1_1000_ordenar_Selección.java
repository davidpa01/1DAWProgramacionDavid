package capitulo05.bloque03;

import metodos.Utiles;

/**
 *1º.- Realiza un ejercicio que inicialice un array de 150 elementos enteros al azar entre 0 y 1.000. 
 *Una vez que lo hayas conseguido debes realizar implementar el algoritmo de ordenación llamado "Burbuja", 
 *con el objetivo de ordenar el array completamente. Puedes consultar el siguiente
 */

public class Ejercicio04_array_150_azar_1_1000_ordenar_Selección {

	/**
	 * Método principal
	 * @param args
	 */
	
	public static void main(String[] args) {
		/*int actual, mas_pequeno, j, temp;
		 
	     for (actual = 0; actual < taille - 1; actual++)
	     {
	         mas_pequeno = actual;
	         for (j = actual 1; j < taille; j++)
	              if (vector[j] < vector[mas_pequeno])
	                  mas_pequeno = j;
	          temp = vector[actual];
	          vector[actual] = vector[mas_pequeno];
	          vector[mas_pequeno] = temp;
	     }*/
		
	}

}
